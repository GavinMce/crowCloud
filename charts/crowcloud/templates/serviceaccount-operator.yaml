apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "crowcloud.fullname" . }}-operator
  labels:
    {{- include "crowcloud.labels" . | nindent 4 }}
    app.kubernetes.io/component: operator
